import 'package:flutter/material.dart';

Widget buildWebAlert(){
  return AlertDialog(
    title: Center(child: Text("Informativo"),),content: Container(height: 150,child: Center(child: Text("Está funcionalidade ainda está sendo estudada, para WEB"),),),
  );
}